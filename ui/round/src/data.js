module.exports = function(opts) {

    var defaults = {
        rack : [{}, {}, {}, {}, {}, {}, {}],
        players : [],
        playerToMove : 1,
        playerNumber : null // our player number
    }

    return defaults;
}
